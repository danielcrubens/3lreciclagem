

// ANIMAÇÃO COM SCROLL
$(document).ready(function () {
  const ScrollDown = ScrollReveal({
    origin: "bottom",
    distance: "45px",
    duration: 960,
    reset: true,
    easing: "ease-out",
    
  });
  ScrollDown.reveal(".scroll-down", { delay: 2 });
  ScrollDown.reveal(".scroll-down-delay-3", { delay: 3 });
  ScrollDown.reveal(".scroll-down-delay-2", { delay: 1 });
  const ScrollLeft = ScrollReveal({
    origin: "left",
    distance: "45px",
    duration: 660,
    reset: true,
    easing: "ease-out",
  });
  ScrollLeft.reveal(".scroll-left", { delay: 1 });
  const ScrollRight = ScrollReveal({
    origin: "right",
    distance: "45px",
    duration: 660,
    reset: true,
    easing: "ease-out",
  });
  ScrollRight.reveal(".scroll-right", { delay: 1 });
});

$('.owl-carousel').owlCarousel({
    loop:true,
    margin:10,
    nav:true,
   autoplay:true, 
    navigation: true,
  navText: [$('.owl-next'),$('.owl-prev')],
    responsive:{
        0:{
            items:1
        },
        600:{
            items:3
        },
        1000:{
            items:4
        }
    }
})
//VOLTAR AO TOPO
$(document).ready(function () {
    $(window).scroll(function () {
      let windowScroll = $(window).scrollTop();
      if (windowScroll > 750) {
        $(".topo").fadeIn(300);
      } else {
        $(".topo").fadeOut(300);
      }
    });
  
    $(".topo").click(function () {
      $("html,body").animate({
        scrollTop: $("html").offset().top,
      });
      return false;
    });
  });

  /* SCROLL SUAVE */
  $('.navbar a[href^="#"]').on('click', function(e) {
     e.preventDefault(); 
    let id = $(this).attr('href'),
        targetOffset = $(id).offset().top;
    /*      $('#meuMenu ul li').click(function(){
          $('#meuMenu ul li').removeClass('active-menu');    
          $(this).addClass('active-menu');    
      });  */
             
    $('html, body').animate({ 
      scrollTop: targetOffset - 80
      
    }, 500);
    
  });
/* MENU MOBILE */
  $(document).ready(function () {

    $('.hamburger-button').on('click', function () {
      $('.animated-icon').toggleClass('open');
    });
  });


  /* $(window).scroll(function() {
		var windscroll = $(window).scrollTop();
		if (windscroll >= 900) {
			$('section').each(function(i) {
        // The number at the end of the next line is how pany pixels you from the top you want it to activate.
				if ($(this).position().top <= windscroll - 580) {
					$('nav li.activo').removeClass('activo');
					$('nav li').eq(i).addClass('activo');
				}
			});

		} else {

			$('nav li.activo').removeClass('activo');
			$('nav li:second ').addClass('activo');
		}

		}).scroll(); */
 
/* MASCARA TEL */
$(function () {
	$("#tel-contato").mask("+99 (00) 0 0000-0000");
});

/* document.addEventListener('wpcf7mailsent', function (Event) {
    location = 'http://seu-dominio.com.br/sua-pagina-customizada';
  }, false); */
